--- /dev/null
+++ hugo/extensions.bzl
@@ -0,0 +1,10 @@
+load("//hugo:rules.bzl", "hugo_repository")
+
+def _hugo_ext_impl(ctx):
+    hugo_repository(
+        name = "hugo",
+        extended = True,
+        version = "0.146.7",
+    )
+
+hugo_ext = module_extension(implementation = _hugo_ext_impl)