--- hugo/internal/hugo_site.bzl
+++ hugo/internal/hugo_site.bzl
@@ -250,5 +250,5 @@
-    if ctx.attr.quiet:
-        hugo_args.append("--quiet")
-    if ctx.attr.quiet:
-        hugo_args.append("--verbose")
+    if ctx.attr.verbose:
+        hugo_args.append("--verbose")
+    elif ctx.attr.quiet:
+        hugo_args.append("--quiet")
     if ctx.attr.disable_fast_render: