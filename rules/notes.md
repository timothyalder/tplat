
`doc_section`

1. Create folder with target name
1. Copy all markdown files into folder. Copy all date into subfolder
    * Run linting, preprocess for Jekyll
> Should support recursive nesting of doc_sections

`doc_publish`

1. Create a folder with target name
1. Build all doc_sections and copy with into folder

`doc_site`

1. Run `doc_publish`, host site locally
